import React from "react";
import MainNav from "./MainNav";

const Header = () => {
	return (
		<header className="nc- sticky top-0 w-full z-40">
			<MainNav />
		</header>
	);
};

export default Header;
