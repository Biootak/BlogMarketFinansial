# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/
.yarnrc.yml

# Next.js
/.next/
/.next-lh/
/out/

# Uploaded files - tracked for deployment fallback
# public/uploads/ is now tracked in git
.screenshots
# Testing
/coverage
.agent_tmp/
tmp/
scripts/*
!scripts/backup-db.mjs
!scripts/restore-db.mjs
!scripts/dev-turbo.mjs
!scripts/clear-dev-cache.mjs
!scripts/setup-cron-jobs.mjs
!scripts/playwright-open.mjs
!scripts/reencrypt-totp-secrets.ts
# cron-job.org browser automation (حاوی session/cookie کاربر — هرگز commit نشود)
.cronjob-profile/
.cronjob-mcp.log
.cronjob-mcp.err
.mcp-cmds/
.agents/skills/*/scripts/
.claude/skills/*/scripts/
# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.tmp-screenshot*
/logs/
tmp-*.log
web-build.log
cron-build.log
debug.log
# Environment Variables
.env
.env.*
!.env.example

# IDE
.vscode/
.kiro/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Cache
.cache/
.turbo/

# Build
*.tgz

# Development tools
.aider*
.opencode/
.worktrees/

# Lighthouse reports (lh-* & lighthouse-* variants from perf runs)
/lh-*.json
/lh-*.html
/lighthouse-*.report.html
/lighthouse-*.report.json
/.lh-tmp/
/.lighthouse-tmp/
/lighthouse-report*.json
/lighthouse-report*.html
/home.html

# Performance audit docs (generated from perf runs)
/PERFORMANCE_ACTION_PLAN.md
/PERFORMANCE_GUIDE.md
/PERF_PLAN.md
/AUDIT_REPORT.md

# Sentry
.sentryclirc
.vercel
.env*

# Large external theme assets / harness metadata
/Orchis-theme/
/WhiteSur-gtk-theme/
/.kimchi/ferments/

# Graphify MCP knowledge graph (regenerated from source; not source-controlled)
/graphify-out/
/.graphify-bak-images/
/.mcp.json

# 2026-07-06: codegraph MCP cache (SQLite DB + daemon state). Regenerated
# from source on demand; tracked nowhere else. The 16MB `codegraph.db`
# alone is enough reason to keep this out of git � it bloats `git status`
# and inflates every shallow clone.
/.codegraph/

# Freebuff Desktop � local database and agent config (user-specific, never commit)
/.freebuff/
/.claude/settings.local.json
/.openclaude/
/.kilo/
/.kimchi/agents.json

# 2026-07-06: Playwright artifacts. The HTML report and per-test JSON
# outputs are regenerated on every `playwright test` run, so they're
# noise in the working tree.
/playwright-report/
/test-results/
/.playwright/
# Playwright MCP snapshots (session-specific accessibility snapshots, regenerated on every session)
/.playwright-mcp/

# TGJU scraper debug HTML dumps (regenerated on demand via TGJU_DEBUG_DUMP=1)
/.tgju-debug/

# Public dev/visual reports (regenerated on demand via scripts/build-tgju-visual.mjs)
/public/dev-tools/

# ============================================================================
# LARGE BINARIES & INSTALLERS
# ???????? ???? � ???? ?? git ?????? (???? ??? USB/share ??? ????? ????)
# ============================================================================
# Linux IDE/editor installers
*.rpm
*.deb
*.AppImage
*.flatpak
*.snap
# Windows installers
*.exe
*.msi
*.msix
# macOS installers
*.dmg
*.pkg
# Generic archives that likely contain binaries
*.iso
*.img
# Node.js tarballs (cached by download-cache.sh)
node-v*.tar.xz
node-v*.tar.gz
node-v*.zip

# ============================================================================
# OFFLINE CACHE (????????? ???? download-cache.sh)
# ????? ??? VirtualBox share / USB ???????? ?? git
# ============================================================================
/offline-cache/node/
/offline-cache/rpms/
/offline-cache/MANIFEST.txt
# ??? .gitkeep tracked ??????? ?? ??????????? ??? ???

# ============================================================================
# AGENT / AI TASK FILES
# ???????? ???? agent ?? � session-specific? ?? source code
# ============================================================================
/Documents/bob-task-*.json
/Documents/task-*.json
/Documents/agent-*.json
/.bob/
/.trae/
/.kilo/tasks/
/.claude/tasks/
# ??? ?? ????? agent ????? ????? ??:
/.agent*/
/agent-tasks/
/.task-*/

# ============================================================================
# RUNTIME / EXTRACTED BINARIES
# ????????? ?? ??? ????? user extract ???????
# ============================================================================
/trae-extracted/
/app-extracted/
*.AppDir/

# ============================================================================
# CODEPUS � local shadow-git and task state (machine-specific, never commit)
# ============================================================================
/.codepus/
.codepus-todos.json

# Linux Trash (GNOME/KDE delete basket � never commit)
/.Trash-*/

# ============================================================================
# PERF / BUILD OUTPUT
# ????? build ??? performance � generated? ?? source code
# ============================================================================
/perf-out/

# ============================================================================
# WINDOWS MACHINE-SPECIFIC
# ============================================================================
# Registry patch files � machine-specific, never commit
bypass.reg
*.reg

# ============================================================================
# MACHINE-SPECIFIC / SESSION TOOLING
# ????? ? ?????? ????? ?? ????? � ????? ?? git ?????
# ============================================================================
# Golden-image provisioning state (regenerated per machine)
/.golden-provisioned
# VM setup scripts + SSH public key (machine-specific)
/.vmsetup/
# Browser/MCP session configs (contain localhost URLs)
/.playwright.json
/args.json
# One-off audit scripts (tied to specific lighthouse report runs)
/a11y-details.cjs
/lcp-deep.cjs
# One-off debug scripts (never commit)
/check-db.js
# Temp / personal notes
ali.md
# Ad-hoc SQL queries / sync test artifacts
/tmp_query.sql
/SYNC_TEST_FROM_GUEST.txt

# ============================================================================
# VM / VIRTUALBOX / VMWARE INFRA SCRIPTS (machine-specific, never commit)
# ============================================================================
auto.ks
bake-golden.sh
bootstrap-live.sh
fedora-live-setup.sh
guest-optimize.sh
optimize-guest.sh
fix-share.sh
diagnose-share.sh
enable-bidirect-sync.sh
bidirectional-sync.sh
install-share-permanent.sh
start-vmware.sh
start-installed.sh
unstack-share.sh
autostart-trae.sh
.bootstrap-launcher.sh
setup-vm.ps1
# generated desktop screenshots in project root (root-only)
/*.png
/*.jpg
/*.jpeg

# UI/UX Pro Max design system (generated by AI agent, session-specific)
/design-system/

# AGENTS.ui-ux-skill.md (generated enforcement file � not source code)
/AGENTS.ui-ux-skill.md

# ============================================================================
# Database backups (never commit dumps or backup logs)
# ============================================================================
/backups/

# Gozunga OpenTofu credentials (deploy/gozunga)
*.openrc
terraform.tfvars
!deploy/gozunga/example-openrc
# Codebuff/agent session plans
.zcode/

# Bundle-audit production snapshot (npm run bundle:audit) — regenerated per audit
/.next-bundle-audit/
# Isolated production build for perf measurement (NEXT_DIST_DIR=.next-audit)
/.next-audit/
public/data/market-rates.json
