# AI AGENT RULES - DO NOT DELETE

To any AI Agent working in this repository: 
Follow the token-optimization policy to save credits and context window.

1. Use `grep` for searching.
2. Use `edit` for code changes.
3. Use `sessions_spawn` (explore) for deep repo analysis.
4. Read files in small chunks (offset/limit).

Refer to `${workspace_memory}/ACCIO.md` for full details.
