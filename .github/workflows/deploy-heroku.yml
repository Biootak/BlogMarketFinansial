name: Deploy to Heroku (DISABLED - migrated to Azure)

# ⛔ 2026-08-16: این workflow غیرفعال شد — دیپلوی به Azure منتقل شد.
# تصویرها توسط .github/workflows/docker-build-push.yml در CI ساخته و به ghcr.io
# push می‌شوند و VM آژور (fm-vm) با cron-poll هر دقیقه pull می‌کند.
# Heroku دیگر سرویس نمی‌دهد؛ برای بازگرداندن این workflow trigger ها را برگردان.

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heroku deploy disabled - site runs on Azure (financialmarket.page)"
