name: Heroku Stability Check (DISABLED - migrated to Azure)

# ⛔ 2026-08-16: این workflow غیرفعال شد — Heroku دیگر سرویس نمی‌دهد.
# سایت روی Azure VM اجرا می‌شود؛ health از طریق cron-poll لاگ و docker
# healthcheck کانتینرها پایش می‌شود.

on:
  workflow_dispatch:

jobs:
  check:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Heroku stability check disabled - site runs on Azure"
